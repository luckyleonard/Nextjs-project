import withRepoHeader from '../../components/withRepoHeader';
import { api_request } from '../../utils/apiHelper';

function IssuesDetail() {
  return <div>Detail issues</div>;
}

export default withRepoHeader(IssuesDetail, 'issues');
