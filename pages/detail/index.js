import withRepoHeader from '../../components/withRepoHeader';
import { api_request } from '../../utils/apiHelper';

function Detail() {
  return <div>Detail index</div>;
}

export default withRepoHeader(Detail, 'index');
